{
  "name": "kspdatathon2026",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd nextjs && npm install --legacy-peer-deps && npm run build && cd .. && cp -R nextjs/.next ./ && (cp -R nextjs/public ./public 2>/dev/null || true) && (mkdir -p .next/standalone/.next && cp -R .next/* .next/standalone/.next/ 2>/dev/null || true)",
    "start": "cd nextjs && npm run start",
    "dev": "cd nextjs && npm run dev"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.18.1",
    "dotenv": "^17.4.2",
    "next": "^15.1.7",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zcatalyst-sdk-node": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0"
  }
}
